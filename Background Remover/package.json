{
  "name": "my_bg_remover",
  "version": "1.0.0",
  "description": "Interactive CLI that strips image backgrounds in bulk using the macOS Vision framework — the same engine as Preview's Remove Background. Offline, no API key, no model download.",
  "bin": {
    "my_bg_remover": "./dist/index.js"
  },
  "scripts": {
    "dev": "ts-node ./src/index.ts",
    "start": "node ./dist/index.js",
    "build": "tsc && npm run build:swift && npm run copy:swift && chmod +x dist/index.js",
    "build:swift": "mkdir -p dist/bin && swiftc -O src/swift/bgremove.swift -o dist/bin/bgremove",
    "copy:swift": "mkdir -p dist/swift && cp src/swift/*.swift dist/swift/"
  },
  "files": [
    "dist/"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@clack/prompts": "^0.10.1",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
